version: "3"
services:
  vova-games:
    build: .
    container_name: vova-games
    ports:
      - "3040:80"
    restart: unless-stopped

  # --- Coop relay (раскомментировать в coop-session1) ---
  # WebSocket relay для кооп-режима. Node.js + ws library.
  # См. docs/superpowers/specs/2026-04-10-coop-design.md
  # relay:
  #   build: ./server
  #   container_name: eldoria-relay
  #   restart: unless-stopped
  #   expose:
  #     - "8080"
  #   networks:
  #     - default
